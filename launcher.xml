<Module>
<ModulePrefs title="Home"/>
<Content type="html">
<![CDATA[ 
<script>
var gameUrls = [
    "https://images-opensocial.googleusercontent.com/gadgets/ifr?url=https://raw.githubusercontent.com/Vadar-X/VGaming/main/main.xml&container=ig"
    ]

  function openGameWindow(gameNumber) {
    return function() {
      var win;
        if (win) {
          win.focus();
        } else {
          win = window.open();
          win.document.body.style.margin = '0';
          win.document.body.style.height = '100vh';
          var iframe = win.document.createElement('iframe');
          iframe.style.border = 'none';
          iframe.style.width = '100%';
          iframe.style.height = '100%';
          iframe.style.margin = '0';
          iframe.src = `data:text/html;charset=utf-8, <iframe src="` + gameUrls[gameNumber - 1] + `" style="margin:0; padding:0; width:100%; height:100%; border:none;"></iframe>`;
          win.document.body.appendChild(iframe);

          var interval = setInterval(function() {
            if (win.closed) {
              clearInterval(interval);
              win = undefined;
            }
          }, 500);
        }
    };
  }

  openGameWindow(1)
</script>
 ]]>
</Content>
</Module>
